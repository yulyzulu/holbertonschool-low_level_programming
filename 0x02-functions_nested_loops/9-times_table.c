#include "holberton.h"
/**
*
*
*
*/
void times_table(void)
{
	int a, b, c, x, y, z;
	x = a * 9;
	y = b * 9;
	z = c * 9;
	
	for (a = 0; a <= 9; a++);
	for (b = 0; b <= 9; b++);
	for (c = 0; c <= 9; c++);
{
	_putchar(x);
	_putchar(y);
	_putchar(z);
	_putchar('\n');
}
	
}

